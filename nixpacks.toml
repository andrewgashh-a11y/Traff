[phases.setup]
nixPkgs = ["python3", "ffmpeg"]

[phases.install]
cmds = ["python3 -m venv --copies /opt/venv && /opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/opt/venv/bin/python app.py"
